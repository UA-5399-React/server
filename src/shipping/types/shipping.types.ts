export interface CityOption {
  ref: string;
  name: string;
  area: string;
}

export interface WarehouseOption {
  ref: string;
  number: string;
  label: string;
  fullAddress: string;
  isPostMachine: boolean;
}
